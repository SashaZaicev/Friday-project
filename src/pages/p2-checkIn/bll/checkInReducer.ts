const initialState = {}
export const checkInReducer = (state= initialState, action: any) => {
    switch (action.type) {
        case "": {
            return {}
        }
        default:
            return state;
    }
};