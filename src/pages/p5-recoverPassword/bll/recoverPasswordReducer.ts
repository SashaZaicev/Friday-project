const initialState = {}
export const recoverPasswordReducer = (state= initialState, action: any) => {
    switch (action.type) {
        case "": {
            return {}
        }
        default:
            return state;
    }
};